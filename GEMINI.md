@AGENTS.md

# Antigravity / Gemini Project Rules

This repository deploys through GitHub/Vercel. Before creating commits or pushing, Antigravity/Gemini must verify and use:

```bash
git config user.name "Vijendrapratap"
git config user.email "44225657+Vijendrapratap@users.noreply.github.com"
```

Do not commit as `mrpratap`, `AI Bot`, `bot@example.com`, `pratap@example.com`, or any generic agent identity. If a wrong author is created before push, fix it before pushing.
